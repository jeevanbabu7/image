# Image Tools Frontend

React (Vite) UI for the Image Tools SaaS app.

## Setup

```bash
cd frontend
npm install
```

## Run (dev)

```bash
npm run dev
```

## Notes

- API base defaults to `http://localhost:4000/api`.
- You can override with `VITE_API_BASE` in a `.env` file.
